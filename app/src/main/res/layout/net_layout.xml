<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#f0f0f0"
    android:orientation="horizontal"
    android:visibility="gone"
    android:padding="@dimen/android_width_15">

    <ProgressBar
        android:id="@+id/net_isneting"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/net_notnet"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/net_warn_icon"
        android:visibility="gone" />

    <TextView
        android:id="@+id/net_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="10dp"
        android:text="网络不给力，请检查网络设置"
        android:textColor="@color/black" />

    <TextView style="@style/tv_h" />

    <ImageView
        android:id="@+id/net_next"
        style="@style/gotonextimg"
        android:layout_gravity="center_vertical" />

</LinearLayout>